name: Debug
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Set up secret file
      run: |
        cat ${{ secrets.PASSWORD }}  
        cat ${{ secrets.USERNAME }}  
        cat ${{ secrets.IP_ADDRESS }}


